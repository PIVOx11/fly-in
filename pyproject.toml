[project]
name = "fly-in"
version = "0.1.0"
description = "Design an efficient drone routing"

readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pygame>=2.6.1",
    "rich>=15.0.0",
]
